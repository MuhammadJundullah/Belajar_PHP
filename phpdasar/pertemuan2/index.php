<?php
    // Pertemuan 2 - PHP dasae
    // sintaks PHP
    
    // standar output
    // bisa menggunakan echo, print
    // untuk mencetak array yaitu print_r
    // var_dump untuk melihat isi dari variabel

    // Penulisan sintaks PHP
    // PHP di dalam HTML
    // HTML di dalam PHP
    // variabel dan tipe data
    // variabel (dalam membuatnya tidak boleh diawali dengan angka, tapi boleh mengandung angka)
    // $nama = "Sayid";
    // echo "nama saya $nama"
  
    // operator
    //  + - * / %
    // $x = 10;
    // $y = 20;
    // echo $x * $y;

    // penggabung string / concatenation / concat
    // .
    // $nama_depan = "Sayid Muhammad";
    // $nama_belakarng = "Jundullah";
    // echo $nama_depan . " " . $nama_belakarng;

    // operator assignment
    // =, +=, -=, *=, %=, .=
    // $x = 1;
    // $x += 2;
    // echo $x
    // $nama = "Sayid";
    // $nama .= " ";
    // $nama .= "Muhammad Jundullah";
    // echo $nama;

    // perbandingan
    // <, >, <=, >=, ==, != (hanya cek nilai nya saja tidak dengan tipe datanya)
    // var_dump (2 < 3);

    // identitas
    // ===, !== (untuk mengecek nilai bahkan tipe datanya sama atau tidak)
    // var_dump(1 === "1")

    // logika
    // $$, ||, !

    // $x = 20;
    // var_dump ($x < 10 || $x % 2 == 0);
?>

